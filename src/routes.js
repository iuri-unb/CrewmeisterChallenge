const express = require('express')
const routes = express.Router()
/*
const MemberController = require('./controllers/MemberController')
const ProjectController = require('./controllers/AbsenceController')

routes
    // Members
    .get('/userId', MemberController.index)
    .post('/members', MemberController.create)
    .put('/members', MemberController.update)
    // Absences
    .get('/absences', ProjectController.index)
    .post('/absences', ProjectController.create)


module.exports = routes
*/